# Telegram Bot для управления инструментами на строительных объектах

Бот для управления инструментами, проведения инвентаризации и координации работ между бригадирами и рабочими на строительных объектах.

## 🚀 Возможности

### Для бригадиров (прорабов):
- ✅ Просмотр и одобрение заявок на регистрацию рабочих
- ✅ Управление инструментами на объекте
- ✅ Проведение инвентаризации с QR-кодами
- ✅ Обработка заявок на передачу инструментов
- ✅ Просмотр рабочих на объекте
- ✅ Генерация XML-отчетов для 1C

### Для рабочих:
- ✅ Просмотр доступных инструментов на объекте
- ✅ Запрос инструментов с других объектов
- ✅ Регистрация на объекте
- ✅ Просмотр информации о бригадире

## 📋 Требования

- Python 3.8+
- PostgreSQL 12+
- Telegram Bot Token

## 🛠️ Установка

### 1. Клонирование репозитория
```bash
git clone <repository-url>
cd Telegram-bot-for-builders
```

### 2. Установка зависимостей
```bash
pip install -r requirements.txt
```

### 3. Настройка базы данных

#### Создание базы данных PostgreSQL:
```sql
CREATE DATABASE construction_bot;
CREATE USER bot_user WITH PASSWORD 'your_password';
GRANT ALL PRIVILEGES ON DATABASE construction_bot TO bot_user;
```

### 4. Настройка переменных окружения

Создайте файл `.env` в корневой директории:

```env
# Telegram Bot
BOT_TOKEN=your_telegram_bot_token

# Database
DB_HOST=localhost
DB_PORT=5432
DB_NAME=construction_bot
DB_USER=bot_user
DB_PASSWORD=your_password
```

### 5. Инициализация базы данных

```bash
python init_db.py
```

Этот скрипт:
- Создаст все необходимые таблицы
- Добавит базовые роли и статусы
- Создаст тестовые данные (3 объекта, 15 инструментов)

## 🚀 Запуск

### Разработка
```bash
python main.py
```

### Продакшн (с использованием systemd)

Создайте файл `/etc/systemd/system/construction-bot.service`:

```ini
[Unit]
Description=Construction Bot
After=network.target

[Service]
Type=simple
User=your_user
WorkingDirectory=/path/to/Telegram-bot-for-builders
Environment=PATH=/path/to/venv/bin
ExecStart=/path/to/venv/bin/python main.py
Restart=always
RestartSec=10

[Install]
WantedBy=multi-user.target
```

Затем:
```bash
sudo systemctl daemon-reload
sudo systemctl enable construction-bot
sudo systemctl start construction-bot
```

## 📖 Использование

### Команды бота

#### Для всех пользователей:
- `/start` - Начало работы с ботом

#### Для бригадиров:
- `/foreman` - Меню бригадира

### Структура меню

#### Меню бригадира:
- 👥 Регистрации на объект
- 👷 Рабочие на объекте  
- 🔧 Инструменты на объекте
- 📦 Заявки на инструменты
- 📋 Провести инвентаризацию

#### Меню рабочего:
- 🔧 Инструменты на объекте
- 📦 Запросить инструмент
- 👷 Информация о бригадире

## 🔧 Функции

### Инвентаризация
1. Бригадир нажимает "Провести инвентаризацию"
2. Отправляет фотографии QR-кодов инструментов
3. Бот обрабатывает фото и распознает QR-коды
4. Генерируется XML-отчет для 1C
5. Статусы инструментов обновляются автоматически

### Заявки на инструменты
1. Рабочий выбирает объект-донор
2. Выбирает доступный инструмент (статус "В наличии")
3. Бригадир получает заявку
4. При одобрении инструмент перемещается на новый объект

### Регистрация рабочих
1. Рабочий регистрируется через `/start`
2. Указывает имя и выбирает объект
3. Бригадир одобряет или отклоняет заявку

## 📊 Структура базы данных

### Основные таблицы:
- `user` - Пользователи (рабочие, бригадиры)
- `object` - Строительные объекты
- `tools` - Инструменты
- `tool_name` - Названия инструментов
- `status` - Статусы инструментов
- `tool_request` - Заявки на передачу инструментов
- `inventory_checks` - Записи об инвентаризации

## 🔒 Безопасность

- Все данные хранятся в PostgreSQL
- Токен бота хранится в переменных окружения
- Пароли базы данных не попадают в код
- Логирование всех действий

## 🐛 Устранение неполадок

### Ошибка подключения к базе данных
```bash
# Проверьте настройки в .env
# Убедитесь, что PostgreSQL запущен
sudo systemctl status postgresql
```

### Бот не отвечает
```bash
# Проверьте токен в .env
# Убедитесь, что бот запущен
sudo systemctl status construction-bot
```

### Ошибки при инвентаризации
- Убедитесь, что QR-коды четкие и хорошо освещены
- Проверьте, что все инструменты имеют QR-коды в базе данных

## 📝 Логи

Логи бота можно посмотреть:
```bash
# Если запущен через systemd
sudo journalctl -u construction-bot -f

# Если запущен вручную
# Логи выводятся в консоль
```

## 🤝 Вклад в проект

1. Форкните репозиторий
2. Создайте ветку для новой функции
3. Внесите изменения
4. Создайте Pull Request

## 📄 Лицензия

MIT License

## 📞 Поддержка

При возникновении проблем создайте Issue в репозитории или обратитесь к разработчику. 